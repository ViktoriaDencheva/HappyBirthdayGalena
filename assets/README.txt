Тук могат да се добавят следните файлове — точно с тези имена:

1. photo.jpg
   Вашата обща снимка. Картичката автоматично ще я покаже
   в елегантната рамка веднага щом файлът се появи тук —
   не е нужно да променяте никакъв код.
   (Ако снимката е вертикална/портретна, ще изглежда най-добре.)

2. music.mp3  (вече качен)
   Фоновата песен. Стартира автоматично при натискане на
   "Виж изненадата", а бутонът горе вдясно позволява да се
   спре/пусне по всяко време. Ако искате друг формат
   (напр. .ogg), сменете разширението в атрибута "src" на
   <audio> тага в index.html.

3. pop.mp3  (по избор)
   Реален запис на "гръмваща тапа". Ако го добавите тук, ще
   се използва вместо синтезирания звук по подразбиране —
   не е нужна промяна в кода.

До добавянето на photo.jpg / pop.mp3 картичката работи напълно
нормално: вместо снимка се показва изящен placeholder текст, а
за тапата се чува синтезиран, но реалистичен звук на пукване.
